# MongoDB skill — E2E test inputs

User messages that should trigger the MongoDB skill (or gracefully handle a missing URI).
These must read like real user messages — no skill/tool names.

- How is NextPost AI doing?
- Show me the latest analytics for nextpostai
- What is the current engagement rate for my campaigns?
- How many posts have been published this month on NextPost AI?
- What is the pulse score trend for nextpostai?
