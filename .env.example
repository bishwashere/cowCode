# Copy to .env and add your keys. .env is gitignored.

LLM_1_API_KEY=

# Optional: Brave Search API for web search (avoids DuckDuckGo blocking).
# Get a key at https://brave.com/search/api/ (Data for Search plan).
# If set, the browser skill uses Brave for search instead of Playwright + DuckDuckGo Lite.
BRAVE_API_KEY=
